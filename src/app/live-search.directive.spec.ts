import { LiveSearchDirective } from './live-search.directive';

describe('LiveSearchDirective', () => {
  it('should create an instance', () => {
    const directive = new LiveSearchDirective();
    expect(directive).toBeTruthy();
  });
});
